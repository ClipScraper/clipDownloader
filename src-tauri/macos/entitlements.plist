<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Allow running our signed sidecar binaries inside the app bundle -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- (Optional) lets users pick files/folders and read/write them (ignored outside Sandbox, harmless here) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Keep Hardened Runtime; no JIT / exec mem needed for your app -->
    <key>com.apple.security.cs.allow-jit</key>
    <false/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <false/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <false/>
  </dict>
</plist>
